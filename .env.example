# ============================================================
# AI Pulse Dashboard — Environment Variables Template
# ZYROX Intelligence System
# ============================================================
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Fill in your credentials
# 3. NEVER commit .env to version control
# ============================================================

# --- Reddit API (Required for better Reddit scraping) ---
# Without these, scraper falls back to public JSON API (slower, limited)
# Setup:
#   1. Go to https://www.reddit.com/prefs/apps
#   2. Click "Create App..." at the bottom
#   3. Name: AI_Pulse_Scraper
#   4. Select "script" type
#   5. Redirect URI: http://localhost:8080
#   6. Click "Create app"
#   7. Copy the ID shown under app name + the secret
REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here
REDDIT_USER_AGENT=AI_Pulse_Dashboard/1.0 by YourRedditUsername

# --- Supabase (Required for cloud sync) ---
# Get these from your Supabase project → Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# --- Dashboard Settings ---
# How many hours back to look for articles (default: 24)
LOOKBACK_HOURS=24
# Port for the local dashboard server
DASHBOARD_PORT=3737
